# pnpm configuration for build environment
# Allow build scripts to run (required for @tailwindcss/oxide native bindings)
# Tailwind CSS v4 uses Rust-based native modules for better performance
enable-pre-post-scripts=true
