node_modules
# bun is the single source of truth (bun.lock); never commit other lockfiles
package-lock.json
pnpm-lock.yaml
yarn.lock
bun.lockb
internal/web/static/js/*.js
internal/web/static/styles.css
.env.local
# Claude Code temporary documentation
claudedocs/
vendor/
/ldap-selfservice-password-changer

# Test coverage
coverage.out
coverage.html
*.coverprofile
tmp/

# Playwright MCP snapshot artifacts
.playwright-mcp/
