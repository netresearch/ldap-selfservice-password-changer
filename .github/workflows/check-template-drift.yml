name: Template Drift

on:
  pull_request:
    branches: [main]
  push:
    branches: [main]

permissions: {}

jobs:
  drift:
    uses: netresearch/.github/.github/workflows/check-template-drift.yml@main
    with:
      template: go-app
    permissions:
      contents: read
