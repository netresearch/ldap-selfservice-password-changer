name: Dependency Review

on:
  pull_request:
    branches: [main]

permissions: {}

jobs:
  review:
    uses: netresearch/.github/.github/workflows/dependency-review.yml@main
    permissions:
      contents: read
      pull-requests: write
