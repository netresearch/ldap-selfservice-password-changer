# If a value is left empty, it will get set to the default during application startup.

PORT="3000"

LDAP_SERVER=""
LDAP_IS_AD=""
LDAP_BASE_DN=""
LDAP_READONLY_USER=""
LDAP_READONLY_PASSWORD=""

MIN_LENGTH=""
MIN_NUMBERS=""
MIN_SYMBOLS=""
MIN_UPPERCASE=""
MIN_LOWERCASE=""
PASSWORD_CAN_INCLUDE_USERNAME=""

# Optional: Dedicated service account for password reset (recommended for security)
# If not set, LDAP_READONLY_USER will be used (backward compatible)
LDAP_RESET_USER=""
LDAP_RESET_PASSWORD=""

# Password Reset Configuration
# Enable password reset feature (default: false)
PASSWORD_RESET_ENABLED=""
# Token validity duration in minutes (default: 15)
RESET_TOKEN_EXPIRY_MINUTES=""
# Max password reset requests per time window (default: 3)
RESET_RATE_LIMIT_REQUESTS=""
# Rate limit time window in minutes (default: 60)
RESET_RATE_LIMIT_WINDOW_MINUTES=""

# SMTP Configuration
# For development with Docker Compose: Use Mailhog (SMTP_HOST=mailhog, SMTP_PORT=1025)
# For production: Use your SMTP server (e.g., smtp.gmail.com:587)
# SMTP server hostname (default: smtp.gmail.com)
SMTP_HOST=""
# SMTP server port (default: 587)
SMTP_PORT=""
# SMTP authentication username (leave empty for Mailhog)
SMTP_USERNAME=""
# SMTP authentication password (leave empty for Mailhog)
SMTP_PASSWORD=""
# Email sender address
SMTP_FROM_ADDRESS=""
# Base URL for password reset links (e.g., https://pwd.example.com or http://localhost:3000)
APP_BASE_URL=""
